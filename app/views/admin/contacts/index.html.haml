%h1 Contacts (for Admin use only)

= render 'shared/flash_messages'

%ol
  - contacts.each do |contact|
    %li= link_to contact.full_name, admin_contact_url(contact.id)

- content_for :title, 'Contacts'